schema_version = 1
name = "Europeana Art Search"
provider = "Europeana"
description = "Search European art and cultural heritage via Europeana API."
action = "schema:SearchAction"
object_types = ["schema:VisualArtwork"]
requires_disclosure = []
returns = ["schema:VisualArtwork"]
source = "Catalog"
llm_instructions = "You are an art history and cultural heritage assistant. Describe artworks, their creators, historical context, and artistic significance."
subagents = []

[endpoint]
url_template = "https://api.europeana.eu/record/v2/search.json?query={query}+TYPE:IMAGE&rows=5&wskey=api2demo"
method = "Get"
response_jsonpath = "$.items[0].title[0]"
response_schema_type = "schema:VisualArtwork"

# Agent-advertised configurable properties (schema.org PropertyValueSpecification)
[[configurable_properties]]
"@type" = "PropertyValueSpecification"
valueName = "results_per_page"
name = "Results Per Page"
description = "Maximum number of results to return per query"
defaultValue = 5
minValue = 1
maxValue = 50
